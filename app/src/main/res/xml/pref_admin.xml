<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="admin_switch"
        android:summary="@string/pref_description_admin"
        android:title="@string/pref_title_admin" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="2"
        android:entries="@array/pref_rule_methodology_list_titles"
        android:entryValues="@array/pref_rule_methodology_list_values"
        android:key="rule_methodology_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_rule_methodology" />

</PreferenceScreen>
